@model IEnumerable<BI.GST.Application.ViewModels.EPIViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Criar", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StatusNome)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.EPIId }) |
            @Html.ActionLink("Detalhar", "Details", new { id=item.EPIId }) |
            @Html.ActionLink("Deletar", "Delete", new { id=item.EPIId })
        </td>
    </tr>
}

</table>

@model BI.GST.Application.ViewModels.SetorViewModel

@{
    ViewBag.Title = "Detalhes";
}

<h2>Detalhes</h2>

<div>
    <h4>Setor</h4>
    <hr />
    <dl class="dl-horizontal">

        <dt>
            @Html.DisplayNameFor(model => model.Nome)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Nome)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.TipoSetor.Nome)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.TipoSetor.Nome)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Descricao)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Descricao)
        </dd>

        <dt>
            @Html.DisplayName("Agente Acidente")
        </dt>
        @if (Model.AgenteAcidentes.Count == 0 || Model.AgenteAcidentes == null)
        {
            <dd>
            </dd>
        }
        else
        {
            foreach (var item in Model.AgenteAcidentes)
            {
                <dd>
                    @Html.DisplayFor(model => item.Nome)
                </dd>
            }
        }
        <dt>

            @Html.DisplayName("Agente Biologico")
        </dt>
        @if (Model.AgenteBiologicos.Count == 0 || Model.AgenteBiologicos == null)
        {
            <dd>
            </dd>
        }
        else
        {
            foreach (var item in Model.AgenteBiologicos)
            {
                <dd>
                    @Html.DisplayFor(model => item.Nome)
                </dd>
            }

        }
        <dt>
            @Html.DisplayName("Agente Ergonomico")
        </dt>
        @if (Model.AgenteErgonomicos.Count == 0 || Model.AgenteErgonomicos == null)
        {
            <dd>
            </dd>
        }
        else
        {
        foreach (var item in Model.AgenteErgonomicos)
            {
            <dd>
                @Html.DisplayFor(model => item.Nome)
            </dd>}
        }
        <dt>
            @Html.DisplayName("Agente Fisico")
        </dt>
        @if (Model.AgenteFisicos.Count == 0 || Model.AgenteFisicos == null)
        {
            <dd>
            </dd>
        }
        else
        {
            foreach (var item in Model.AgenteFisicos)
            {
                <dd>
                    @Html.DisplayFor(model => item.Nome)
                </dd>}
        }
        <dt>
            @Html.DisplayName("Agente Quimico")
        </dt>
        @if (Model.AgenteQuimicos.Count == 0 || Model.AgenteQuimicos == null)
        {
            <dd>
            </dd>
        }
        else
        {
            foreach (var item in Model.AgenteQuimicos)
            {
                <dd>
                    @Html.DisplayFor(model => item.Nome)
                </dd>}
        }

    </dl>
</div>
<p>
    @Html.ActionLink("Editar", "Edit", new { id = Model.SetorId }) |
    @Html.ActionLink("Voltar para lista", "Index")
</p>

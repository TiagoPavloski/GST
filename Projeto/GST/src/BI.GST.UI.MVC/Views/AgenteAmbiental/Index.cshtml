@model IEnumerable<BI.GST.Domain.Entities.AgenteAmbiental>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Delete)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Delete)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AgenteAmbientalId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AgenteAmbientalId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AgenteAmbientalId })
        </td>
    </tr>
}

</table>
